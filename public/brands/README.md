Drop a brand's mark here as `<slug>.svg` or `<slug>.png` and the hero row picks it up
on the next build. Slugs: salesforce, burger-king, centerbase, examsoft, openclinica,
taxrise-inc, alokito-teachers, t-mobile, at-t. Until a file exists, the name is shown.
